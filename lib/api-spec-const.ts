export { API_BASE, SDKS, SDK_COVERAGE, SPEC_URL } from './api-spec';
export const API_ROOT_NOTE =
    'Every endpoint on this page is generated from the live API specification at ' +
    'events.vihaya.app/api/v1/openapi.json, so it cannot drift from the running service.';
